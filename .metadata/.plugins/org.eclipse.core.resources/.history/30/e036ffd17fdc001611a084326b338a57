spring:
     application:
           name:    config-service

     cloud:
       config:
         server:
          git:
           uri: https://github.com/mycapgemini/FirstRepository/blob/master.git

server:
  port: 8989